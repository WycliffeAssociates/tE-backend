version: '3.2'
services:
  web:
    build: .
    container_name: te_unit_tests
    command: bash -c "python3 /tE-backend/tRecorderApi/manage.py jenkins --enable-coverage --settings=tRecorderApi.settings_test"
    depends_on:
      - db
  db:
    image: postgres
    container_name: te-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
